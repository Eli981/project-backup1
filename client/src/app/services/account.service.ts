import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class AccountService {

  // constructor(private http: HttpClient) { }

  // registerUser(userInput:RegisterUser): observable<Account> {
  // return this.http.post < Account > ('http://localhost:5001/api/account/register',userInput).pipe{
  // map(user => user)
}
//   };
// }
// }
// 



//az service baraye sepraion of consernse esttefade mishavad.. 